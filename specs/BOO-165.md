# BOO-165 — /sprint-run: Post-Story-Gate-Assertion (meta.json maschinell verifizieren)

## Summary
`/sprint-run` bekommt einen neuen **Schritt 4.5b „Post-Story-Gate-Assertion"** zwischen Remote-CI-Wait (4.5) und Merge (4.6). Nach jedem `/implement`-Lauf liest der Orchestrator die `meta.json` des Story-Runs und failt, wenn ein `skipped_gates`-Eintrag nicht legitim ist (weder durch `change_type`/Non-Code gedeckt noch in `override_audit` belegt) oder wenn `meta.json` fehlt. Merge nur bei grüner Assertion. Härtet die bisher rein prompt-getriebene Gate-Durchsetzung (Ebene 1) mit einer maschinellen Verifikation gegen den Ground-Truth-Output.

## Why
`/sprint-run` (BOO-157) garantiert Tests/Linter über zwei Ebenen: Delegation an `/implement` (Quality-Gates, prompt-getrieben) und das Remote-CI-Gate vor Merge (BOO-148, mechanisch). Zwischen beiden klafft eine Lücke: ein fehlbarer Agent könnte ein lokales Gate überspringen, ohne dass der Orchestrator es bemerkt. `/implement` schreibt aber in Schritt 6f-bis eine `meta.json` (BOO-36/84) mit `skipped_gates`, `change_type`, `override_audit` — der maschinenlesbare Beleg. Die Assertion schließt die Lücke.

## What
- **`sprint-run/SKILL.md` + `.en.md`**: neuer Schritt 4.5b (Tabellenzeile im Daemon-Loop + dedizierter Abschnitt mit „legitime vs. unbegründete Skips"-Tabelle); Version **1.0.0 → 1.1.0**; Dateistruktur um `references/gate-assertion.md` erweitert.
- **`sprint-run/references/gate-assertion.md` + `.en.md`**: Assertion-Logik, Legitimitäts-Matrix, Pseudocode.
- **`sprint-run/README.md` + `.en.md`**: Schritt-Tabelle + Gate-Sicherheits-Abschnitt um die Assertion erweitert; Version 1.1.0.
- **`HANDBUCH.md` + `.en.md` Anhang/Appendix AD**: Daemon-Loop-Tabelle um 4.5b ergänzt, Abschnitt „Gate-Assertion" + Fehlerbehandlung („unbegründeter Skip → Story zurück").
- **`docs/runbooks/sprint-run.md` + `.en.md`**: Fehlerszenario „stiller/unbegründeter Gate-Skip → Story zurück" in die Tabelle.
- **Sketches** `sprint-run-flow` + `story-breakdown` (DE+EN, `.excalidraw` + `.png`, render-loop-verifiziert): Assertion-Knoten zwischen CI-Wait und Merge.
- **Release-Note** `docs/releases/wave-bk-gate-assertion.md` + `.en.md`.

## Acceptance Criteria
1. Assertion liest `meta.json` pro Story; unbegründeter `skipped_gates`-Eintrag → Story-Fail (zurück auf Backlog) + Operator-Notify (Story-ID + Gate). ☐
2. Legitimität = durch `change_type` (Non-Code 5.7) gedeckt **oder** in `override_audit` belegt. ☐
3. Fehlende `meta.json` → Fail. ☐
4. Non-Code-Story: zusätzlich Evidenz für 6c/6d/6e geprüft. ☐
5. Merge (4.6) erst nach grüner Assertion. ☐
6. SKILL/README v1.1.0 (docs-drift-Parität); HANDBUCH + Runbook + 2 Sketches DE+EN nachgezogen. ☐
7. `docs-drift-check` grün; keine toten Links. ☐

## Constraints
- `/implement`, `/backlog`, `/sprint-review` unverändert — nur `/sprint-run`.
- KEINE naive „skipped_gates leer"-Regel (Non-Code-Skips sind legitim).
- DE+EN-Parität Pflicht. `change_type: tooling`.

## Abgrenzung
- Wave-Buchstabe **bk** (bi = sprint-run BOO-157, bj = role-runbooks BOO-158, bh = knowledge-onboarding-sketches).
- Repo-Slot BOO-165 = Linear BOO-165 (Linear-first).
- Enthält außerdem einen Hotfix: Nachbar-Wave `role-runbooks` von `bi` → `bj` umbenannt (Cross-Session-Kollision mit dem bereits released `wave-bi-sprint-run` aufgelöst).

## Execution Isolation
```yaml
estimate: 2
token_estimate: 18000
execution_mode: sub-agents
worktree_strategy: git-worktree
```

## Session-Referenz
- Branch: `feat/boo-165-gate-assertion`
- Session: /implement, 2026-06-06

## Result
_(nach Abschluss ergänzen: Commits, PR, Wave BK)_
