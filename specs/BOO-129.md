# BOO-129 — Repo-docs Leichtgewicht-SecondBrain: Session-Start-Routine in CLAUDE.md + DE/EN-Doku + Sketch

## Summary
Die generierte `CLAUDE.md` bekommt eine **Session-Start-Routine** (PMO-Hub + neueste `meetings/`/
`decisions/` lesen → Stand zusammenfassen) + **Schreib-Konvention** (Minutes → `meetings/`, Entscheidungen
→ `decisions/`). Damit wird `repo-docs` ein nutzbares Leichtgewicht-SecondBrain ohne Obsidian. Plus
DE/EN-Doku + Render-Loop-Sketch.

## Why
Probelauf-Folgefrage „Kunde hat nichts" → in-repo SecondBrain. `repo-docs` legt bereits PMO-Hub + `decisions/`
+ `meetings/` + `research/` an, aber die generierte `CLAUDE.md` hatte **keine Session-Start-Routine** —
Claude las den Stand beim Start nicht. Genau das macht aus den Markdown-Ordnern erst ein „Brain".

## What
- **`references/file-templates.md` + `.en.md`, CLAUDE.md (Minimum)-Template:** neue Sektion
  „Session-Start-Routine (Leichtgewicht-SecondBrain, BOO-129)" — PMO-Hub + `meetings/`/`decisions/` lesen,
  Stand zusammenfassen, Schreib-Konvention; Pfade für `repo-docs`, sinngemäß `obsidian`/`external-dms`.
- **`references/project-documentation-ssot.md` + `.en.md`:** Loop-Doku + Sketch-Embed nach der repo-docs-Struktur.
- **Sketch** `docs/assets/boo-129-leichtgewicht-secondbrain.{excalidraw,png}` (DE) + `.en.*` — 4-Schritt-Loop
  (Start liest Stand → arbeiten → zurückschreiben → nächste Session), OWLIST-Farben, Render-Loop, Read-verifiziert.

## Acceptance Criteria
1. Generierte `CLAUDE.md` (repo-docs; sinngemäß obsidian) mit Session-Start-Routine. ✓
2. Schreib-Konvention: Minutes → `meetings/`, Entscheidungen → `decisions/` (ADRs). ✓
3. DE/EN-Doku + Sketch des Leichtgewicht-SecondBrain-Loops. ✓
4. DE+EN-Parität. ✓

## Result
Umgesetzt im Branch `feat/boo-docs-secondbrain`. Sketch DE+EN gerendert + Read-verifiziert (Iter 1→2:
Umlaut/Pfeil-Politur). DE+EN.
