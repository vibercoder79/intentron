# BOO-133 — Linear-MCP-auf-VPS-Runbook (OAuth via SSH-Port-Forward) + Token-Setup

## Summary
Neuer HANDBUCH **Anhang AB**: Schritt-für-Schritt, wie man den Claude-Code-Linear-MCP auf einer
**headless VPS** per OAuth verbindet (SSH-Local-Port-Forward für den `localhost:51658`-Callback),
inkl. „Tools erst nach Session-Neustart"-Gotcha und Token-Setup ohne Leak. Plus §3-Korrektur des
Token-Orts und §8g-Deeplink.

## Befund (verifiziert 2026-06-03)
§8g deckt nur Linear↔GitHub. Der MCP-OAuth-auf-VPS, der SSH-Tunnel, `claude mcp auth linear-server`,
die Session-Neustart-Gotcha und der Token-Ort fehlten. §3 nannte „Settings → API" — real (Operator-
verifiziert) **Settings → Security and Access**.

## What
- **HANDBUCH Anhang AB** (DE+EN): Problem (Callback localhost), Lösung (`ssh -L 51658:localhost:51658`),
  Ablauf (Tunnel offen → `claude mcp auth linear-server` → URL lokal öffnen → Callback durch Tunnel →
  Session-Neustart mit offenem Tunnel), Token-Setup ohne Leak (`.env`/`export`, kein Key im Chat),
  Verbindungstest.
- **§3** (DE+EN): Token-Ort „Settings → API" → „Settings → Security and Access" (Tabelle + Account-Bullet).
- **§8g** (DE+EN): Deeplink auf Anhang AB.
- TOC + §13-Wegweiser (DE+EN): Anhang AB ergänzt (28 Anhänge).

## Acceptance Criteria
1. Runbook mit SSH-Tunnel-OAuth + Session-Neustart-Gotcha + `claude mcp auth`. ✓
2. Token-Setup ohne Leak (Terminal/Env, kein Cloud-Chat-Key). ✓
3. §3-Korrektur Token-Ort. ✓  4. §8g/Bootstrap deeplinkt. ✓  5. DE+EN. ✓

## Result
Branch `feat/boo-doku-haertung`. Größte der Doku-Lücken geschlossen; Token-Setup an die Secrets-Policy (`.env` gitignored) gekoppelt.
