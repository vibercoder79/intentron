# BOO-118 — GitHub-Runbook: git remote set-url HTTPS→SSH ergänzen

## Summary
Den Spezialfall „bestehendes HTTPS-Remote auf SSH umstellen" (`git remote set-url origin git@github.com:…`)
als kurzen Schritt 5 im SSH-Abschnitt des HANDBUCH ergänzen (DE+EN), inkl. `git remote -v`-Check und
Verbindungstest `ssh -T git@github.com`.

## Why
Rest von Probelauf-Punkt #4: GitHub-Setup ist sonst gut dokumentiert (SSH-Key, Branch-Protection,
Remote-Add), nur die HTTPS→SSH-Umstellung fehlte — kleine Doku-Lücke. BOO-123 verweist bereits auf
„HTTPS→SSH siehe BOO-118".

## What
- `HANDBUCH.md` + `.en.md` §3 „SSH für GitHub": Schritt 5 — `git remote set-url origin git@github.com:<owner>/<repo>.git`
  + `git remote -v` + `ssh -T git@github.com`.

## Acceptance Criteria
1. HTTPS→SSH-Umstellung als kurzer Schritt im GitHub-Abschnitt (DE+EN), inkl. `ssh -T git@github.com`. ✓

## Result
Umgesetzt im Branch `feat/boo-docs-secondbrain`. Schließt den BOO-123-Forward-Verweis. Trivial, DE+EN.
