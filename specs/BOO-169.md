# BOO-169 — Claude-Code-Modus-Empfehlung auf alle Skills ausrollen (BOO-168-Folge) + grafana/security-architect-Lücken (DE+EN)

## Summary
BOO-168 hat die Claude-Code-Berechtigungs-Modus-Empfehlung etabliert (HANDBUCH §6 + README-Modus-Zeilen), aber nur für `/implement` und `/sprint-run`. Diese Story rollt das **gleiche Muster** (README-Modus-Zeile + §6-Tabelle, **kein** neues Frontmatter-Feld) auf die 13 übrigen Skills aus und schließt zwei beim Review gefundene Lücken. Reine Doku — bis auf den security-architect-Frontmatter-Fix kein Versions-Bump.

## Why
Genau die Skills, die ein Operator beim Aufruf zuerst sieht (`/ideation`, `/intent`, …), trugen noch keine Modus-Empfehlung. Operator-Frage: „Wenn ich /ideation oder /intent aufrufe — welcher Modus?" Zusätzlich beim Review entdeckt: `security-architect/SKILL.md` hat **kein** `recommended_model`/`version:` im Frontmatter (obwohl security-kritisch), und `/grafana` führt `update_dashboard(overwrite=true)` **ohne** skill-eigenes Confirm-Gate aus.

## What
- **13 README-Modus-Zeilen (DE+EN)** im BOO-168-Stil (`> **Claude-Code-Modus:** … Details: HANDBUCH §6`): ideation, intent, pitch, knowledge-onboarding, sprint-review, backlog, architecture-review, security-architect, dpo, visualize, cloud-system-engineer, grafana, bootstrap.
- **HANDBUCH §6 (DE+EN)**: Beispiel-Spalte um alle Skills erweitert + neue vierte Zeile „Externe/irreversible Writes → `default` (Ask before edits)" (für `/grafana` + `/cloud-system-engineer` Modus C) + Verweis, dass die Tabelle die zentrale Quelle ist.
- **Lücke security-architect**: `recommended_model: opus` + `version: 1.1.0` ins Frontmatter (DE+EN); security-architect zusätzlich in `bootstrap/references/model-tiers.json` opus.default_for_skills.
- **Lücke grafana**: Operator-Confirm-Blockquote vor `overwrite=true` in SKILL.md (DE+EN).

## Acceptance Criteria
1. Alle 13 Skills haben eine README-Modus-Zeile (DE+EN), Verweis auf HANDBUCH §6. ☐
2. HANDBUCH §6-Tabelle (DE+EN) um vierte Zeile + alle Skills erweitert. ☐
3. security-architect Frontmatter: recommended_model + version (DE+EN); model-tiers.json ergänzt. ☐
4. grafana SKILL (DE+EN): Confirm-Schritt vor overwrite=true. ☐
5. `docs-drift-check` grün; kein Versions-Bump außer security-architect-Frontmatter-Fix. ☐
6. Release-Note DE+EN. ☐

## Constraints
- Konsistent mit BOO-168: phasen-basierte Tabelle + README-Zeile, **kein** neues Frontmatter-Feld (kein Doppel-Owner). DE+EN-Parität. `change_type: content`. Anti-Fabrikation: Modus-Zuordnung folgt der dokumentierten Skill-Charakteristik.
- Vokabular: `plan` · `acceptEdits` · `dontAsk` + Allowlist · `bypassPermissions` (nur Isolation) · `default` (externe irreversible Writes).

## Abgrenzung
- Wave-Buchstabe wird spät gegen `docs/releases/` vergeben (Cross-Session-Drift-Guard). Repo-Slot BOO-169 = Linear BOO-169.
- Folge zu BOO-168 (sprint-run/implement bereits abgedeckt — hier nicht erneut angefasst).

## Execution Isolation
```yaml
estimate: 3
token_estimate: 18000
execution_mode: sub-agents
worktree_strategy: git-worktree
```

## Session-Referenz
- Branch: `feat/boo-169-claude-code-modus-alle-skills`
- Session: /implement-äquivalent (Lead-orchestriert), 2026-06-06

## Result
_(nach Abschluss ergänzen)_
