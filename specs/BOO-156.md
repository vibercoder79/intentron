# BOO-156 — Runbook `framework-update.md` (Bestandsprojekt hochziehen) + README-Verlinkung

## Summary
Bündelt den „Framework-Update auf einem Bestandsprojekt"-Ablauf in einen dedizierten Copy-Paste-Runbook (`docs/runbooks/framework-update.md` + `.en.md`), analog `secondbrain-nachziehen.md`, und verlinkt ihn aus dem README (EN + DE). Folge-Story zu BOO-155.

## Why
Bisher war der Ablauf auf zwei dokumentierte Hälften verteilt (Skill-Update-Befehl in HANDBUCH-FAQ/README; Upgrade-Modi in `framework-upgrade.md`/§7.5a). Ein einzelner Runbook macht aus „zusammengesetzt aus zwei Stellen" ein „dokumentiert als ein Stück".

## What
- **Neu: `docs/runbooks/framework-update.md` + `.en.md`** — Schritt 1 (Bootstrap-Skill via sparse-checkout aktualisieren) + Schritt 2 (`/bootstrap` → inspect/apply-safe/apply-with-confirmation, §7.5a-Auto-Erkennung), Ein-Klick-Prompt, Sicherheit/Idempotenz, Verweise.
- **`README.md`**: Runbook im EN-Abschnitt „C) AI self-update for an old / brownfield install" und im DE-Pendant verlinkt.

## Acceptance Criteria
1. Runbook DE+EN vorhanden, nutzt `/bootstrap` (kein `--update`). ✓
2. README verlinkt Runbook in EN **und** DE. ✓
3. `docs-drift-check` grün; keine toten Links. ✓
4. Release-Note Wave BG (DE+EN). ✓

## Abgrenzung
- Reine Doku, kein Code, kein Skill, kein Sketch-Asset (Runbook referenziert kein Bild).
- Wave-Buchstabe **bg** (bf gehört BOO-155 — Cross-Session-Drift-Guard nach BOO-153 beachtet).
- Repo-Slot BOO-156 = Linear BOO-156 (Linear-first). Baut auf BOO-144 (Runbook-Muster) + BOO-155 (korrigierte Upgrade-Doku) auf.

## Result
Umgesetzt im Branch `tobiaschschmidt/boo-156-docs-runbook-framework-updatemd-bestandsprojekt-auf`. DE+EN.
