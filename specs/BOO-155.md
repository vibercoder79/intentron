# BOO-155 — Upgrade-Doku nachziehen: irreführendes `/bootstrap --update` + Artefakt-/migrate-Lücken

## Summary
Der dokumentierte User-Upgrade-Pfad ist im *Mechanismus* aktuell (3 Modi inspect/apply-safe/apply-with-confirmation, `migrate-to-v2.sh --issue BOO-NN`, Release-Waves), aber die *Einstiegs-Doku* ist hinter den Wellen BOO-91/108/130–149 zurückgeblieben. Eine Stelle (`/bootstrap --update`) ist aktiv irreführend. Reine Doku-Korrektur, kein Runtime-Code.

## Why
Das englische Root-`README.md` beschreibt den modernen Upgrade-Weg bereits korrekt; `HANDBUCH`-FAQ und `framework-upgrade.md` hinken hinterher. Wer der FAQ folgt, tippt ein Flag (`--update`), das `bootstrap/SKILL.md` gar nicht kennt — der echte Trigger ist die Auto-Erkennung der bestehenden Installation in §7.5a.

## What
- **`HANDBUCH.md` + `.en.md` (FAQ „Wie aktualisiere ich die Skills"):** `/bootstrap --update` → `/bootstrap`; Kommentar erklärt Auto-Erkennung + Modus-Abfrage (inspect/apply-safe/apply-with-confirmation), Verweis auf `framework-upgrade.md`.
- **`bootstrap/references/framework-upgrade.md` + `.en.md`:**
  - „Dateikategorien"-Tabelle um `CONTEXT.md`, `solution-artefakte.md`, `DEVELOPER_ONBOARDING.md`, `.claude/environment.json` (+ Generator) und `docs/kollisionsschutz-drei-ebenen.md` ergänzt (jeweils „nur seeden wenn fehlend, idempotent").
  - Nutzerfluss: `bash bootstrap/scripts/migrate-to-v2.sh --issue BOO-NN` als idempotenten Bestands-Migrationspfad verankert.
  - Schritt 8 verlinkt `bootstrap/references/provider-postflight.md`.

## Acceptance Criteria
1. HANDBUCH DE+EN: kein `/bootstrap --update` mehr, korrekter §7.5a-Ablauf. ✓
2. framework-upgrade DE+EN: 5 neue Artefakt-Zeilen in „Dateikategorien". ✓
3. framework-upgrade DE+EN: migrate-to-v2.sh-Mechanismus + provider-postflight-Verweis. ✓
4. DE/EN-Parität (docs-drift Check 3), keine toten references-Links (Check 1). ✓
5. Release Note (DE+EN); docs-drift grün.

## Abgrenzung
- Kein echtes `/bootstrap --update`-Flag gebaut — Entscheidung: **streichen statt bauen**, weil §7.5a die Funktion leichtgewichtig abdeckt (Code-Crash-Leichtgewicht-Prinzip).
- `bootstrap/README.md` „Version 3.0"-Header bewusst unangetastet: Major-Label, nicht im gate-geprüften Format `**Version:**` → kein docs-drift-Befund.
- Kein Migrations-Skript-Code geändert. Repo-Slot BOO-155 = Linear BOO-155 (deckungsgleich, Linear-first).

## Result
Umgesetzt im Branch `tobiaschschmidt/boo-155-docs-upgrade-doku-nachziehen-irrefuhrendes-bootstrap-update`. DE+EN.
