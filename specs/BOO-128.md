# BOO-128 — Phase 7.6: verify-setup-Re-Run-Hinweis + Prompt am Bootstrap-Ende

## Summary
Die „Nächste Schritte"-Box (Phase 7.6) bekommt den **Re-Run-Befehl für `verify-setup.sh`** + einen
fertigen Operator-Prompt — damit der Operator sein Setup **jederzeit selbst** prüfen kann (read-only,
unabhängig vom `/implement`-Probelauf). Kein neuer Sketch (Anhang-T-Sketch existiert).

## Why
Probelauf-Folgefrage (Mini-Story): `verify-setup.sh` läuft bereits während des Bootstraps (Phase 7.3b)
und ist in Anhang T + Sketch dokumentiert — aber Phase 7.6 nannte nur VS-Code/Claude-Start/`/ideation`,
nicht den Re-Run-Befehl.

## What
- `bootstrap/SKILL.md` + `.en.md`, Phase 7.6: Listenpunkt 5 „Setup jederzeit prüfen: `bash scripts/verify-setup.sh`
  (`--strict` für CI) — read-only, PASS/WARN/FAIL. Details + Sketch: HANDBUCH Anhang T." + Callout mit
  fertigem Operator-Prompt „führ `verify-setup.sh` aus und erklär mir die WARN/FAIL-Punkte".

## Acceptance Criteria
1. Phase 7.6 nennt den Re-Run-Befehl (+ `--strict`, read-only, PASS/WARN/FAIL, Anhang-T-Verweis). ✓
2. Fertiger Operator-Prompt aufgenommen. ✓
3. DE+EN. ✓

## Result
Umgesetzt im Branch `feat/boo-docs-secondbrain`. Kein neuer Sketch nötig (verweist auf bestehenden
`docs/assets/post-install-verification(.en).png`). Verwandt mit BOO-114 (Pre-Flight). DE+EN.
