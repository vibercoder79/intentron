# BOO-151 — Multi-User-VPS-Setup: Doku-Schärfung + Teammitglied-Checkliste + Drei-Ebenen-Kollisionsschutz

## Summary
Konsolidiert + schärft die Doku für **Multi-User-VPS-Betrieb** (mehrere Menschen, ein Projekt) und macht das **Drei-Ebenen-Kollisionsschutz-Modell** explizit. Trennt sauber: Bootstrap-Setup ≠ Entwicklungs-Governance ≠ Git-Best-Practices. DE+EN + Sketch.

## Die drei Parallelitäts-Ebenen
1. **Multi-USER** — mehrere Menschen, ein Projekt → eigener Klon pro User-Home, Sync über GitHub.
2. **Multi-SESSION** — eine Person, 2 Sessions, ein Klon → `git worktree` / Session-Hinweis.
3. **Multi-AGENT** — eine Session, mehrere Sub-Agenten an einer Story → `EXECUTION_ISOLATION` (existiert, BOO-52).

## What
- **`docs/kollisionsschutz-drei-ebenen.md` (+ `.en.md`):** neuer konsolidierter Doku-Abschnitt — die drei Ebenen, je Problem/Schutz/Verweis.
- **`docs/runbooks/multi-user-vps.md` (+ `.en.md`):** Teammitglied-Checkliste, gegliedert nach **einmal VPS** (useradd/SSH/sudo/Skill-Pool) · **einmal GitHub** (Collaborator/Deploy-Key) · **einmal Claude** (~/.claude Skills+.env) · **pro Repo** (clone + Hooks + environment.json). Mit Sketch-Embed.
- **Sketch `docs/assets/multi-user-vps.{excalidraw,png}` (+ `.en.*`):** Multi-User-Klon-Modell (GitHub = Original; eigener Klon pro User-Home; Sync via Git). OWLIST, Render-Loop, Read-verifiziert.
- **HANDBUCH Anhang P §3 Schritt 7 (DE+EN):** „Klon" vs. „git worktree" sauber getrennt (war vermischt) + Runbook-/Konzept-Verweis.
- **`bootstrap/SKILL.md` (+ `.en.md`):** A.7 geschärft — Solo-Hinweis (Ebene 2: `git worktree` bei zwei Sessions) + Multi-User-VPS-Verweis auf Runbook + `journal/daily`-`.gitignore`-Hinweis. `bootstrap` 3.38.0 → 3.39.0.
- **`bootstrap/references/file-templates.md` (+ `.en.md`):** `.gitignore`-Template — kommentierte `journal/daily/`-Zeile, bei Multi-User aktivierbar (Solo: committet).

## Acceptance Criteria
1. Drei-Ebenen-Modell als Framework-Doku-Abschnitt (DE+EN). ✓
2. Multi-User-Runbook mit Teammitglied-Checkliste (4 Spalten) + Sketch (DE+EN). ✓
3. Anhang P §3 Begriffsfix Klon/Worktree (DE+EN). ✓
4. Bootstrap A.7: Solo-Lock-Hinweis + Multi-User-Verweis (DE+EN). ✓
5. `journal/daily/`-`.gitignore`-Konvention für Multi-User (DE+EN). ✓
6. DE+EN-Parität; docs-drift-Check grün. ✓

## Abgrenzung
Multi-AGENT (Ebene 3) ist NICHT Teil — existiert schon (`EXECUTION_ISOLATION`, implement-Schritt-0c). Baut auf BOO-138/139/145 (Maschinen-Ebene) auf.

## Result
Umgesetzt im Branch `feat/boo-151-multi-user-vps`. DE+EN.
