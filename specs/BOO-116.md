# BOO-116 — Stack-Frage "Anderes" → Freitext-Rückfrage statt stillem ESLint-Default

## Summary
Wählt der Operator in A.1 `e) Anderes`, stellt der Bootstrap eine **Freitext-Rückfrage** „welche
Technologie/Sprache?" und gibt einen **passenden Linter-Hinweis** (Go→golangci-lint, Rust→clippy,
Java→Checkstyle+SpotBugs, PHP→PHPStan, Ruby→RuboCop …) — statt still auf den generischen
`eslint.config.mjs`-Default zu fallen. „Noch unklar" wird explizit als offen markiert.

## Why
Probelauf-Punkt #2: `e)` war in der Linter-Tabelle direkt `eslint.config.mjs (generisch)` zugeordnet,
ohne Zwischenfrage — bei Nicht-JS-Projekten (Go/Rust/Java) schlicht falsch. (BOO-127 hatte die e)-Zeile
bereits auf „bzw. Guided Discovery" erweitert; BOO-116 ergänzt den **direkten Tech-Freitext** für den
Fall, dass der Operator die Tech kennt, sie aber nicht a–d ist.)

## What
- `bootstrap/SKILL.md` + `.en.md`, A.1: e)-Zeile auf „Freitext-Rückfrage (BOO-116) bzw. Guided Discovery —
  kein stiller ESLint-Default" geändert; darunter neuer Freitext-Block „Welche Technologie/Sprache?" +
  Linter-Hinweis-Tabelle (Go/Rust/Java/PHP/Ruby/sonstige). `STACK_CHOICE` bleibt `e`, Tooling folgt dem
  Hinweis (ADR), nicht ESLint. „noch unklar" → explizit offen markieren ODER A.1a Guided Discovery.

## Acceptance Criteria
1. Auswahl `e)` löst Freitext-Rückfrage „welche Technologie/Sprache?" aus. ✓
2. Antwort fließt in Linter-/Tooling-Bestimmung (Linter-Hinweis statt stillem ESLint). ✓
3. „noch unklar" explizit markiert, nicht als JS angenommen. ✓

## Result
Umgesetzt im Branch `feat/boo-stack-mcp-branching` (auf der Bündel-1-Spitze). Sauber abgegrenzt zu
BOO-127/A.1a: BOO-116 = direkter Tech-Freitext bei bekannter Nicht-a–d-Tech; A.1a = „unsicher → Vorschlag
a–d". Beide verlinken sich gegenseitig. DE+EN.
