# BOO-145 — Bootstrap Block A: Maschinen-Kontext-Abschnitt in ~/.claude/CLAUDE.md

## Summary
Der Bootstrap schreibt am Ende von **Block A** (neuer Schritt A.8) automatisch einen `## Maschinen-Kontext`-Abschnitt in die globale `~/.claude/CLAUDE.md` — **idempotent** (nur wenn noch nicht vorhanden) und **ohne separaten Operator-Schritt**. Inhalt: OS-Typ (via `uname -s`/`$OSTYPE`), Framework-Version (`intentron <VERSION>` via neuestem Git-Tag), Stack-Praeferenz (aus A.1), verfuegbare Skills (`ls ~/.claude/skills/`).

## Why
Eine Maschine, die das Framework hostet, sollte ihren eigenen Kontext kennen — das gibt jeder KI-Session sofort Orientierung (OS, Framework-Version, bevorzugter Stack, Skills), ohne Extra-Schritt. Ergaenzt `PROJECTS_ROOT` (BOO-138) als zweiten Maschinen-Ebene-Baustein der globalen `~/.claude/CLAUDE.md`.

## Befund (verifiziert 2026-06-04)
- Block A (A.1–A.7) initialisiert den Projekt-Kern; die globale `~/.claude/CLAUDE.md` bekommt aber **keinen** Maschinen-Kontext.
- `STACK_CHOICE`/`LANG_VARIANT` werden in A.1 gemerkt → am Block-A-Ende verfuegbar.
- Keine zentrale VERSION-Datei → Framework-Version via neuestem Release-Tag (`git describe --tags`).

## What
- **`bootstrap/SKILL.md` (+ `.en.md`):** neuer Schritt **A.8 Maschinen-Kontext** vor dem Phase-1-Checkpoint — idempotenter Schreibvorgang (Read-vor-Edit, nicht ueberschreiben), Wert-Ermittlung (OS/Version/Stack/Skills), Abschnitts-Template. `bootstrap` 3.36.0 → 3.37.0.
- **`bootstrap/references/global-registry-update.md` (+ `.en.md`):** neue Sektion „3b. Maschinen-Kontext" — Lese-/Schreib-Regel, Idempotenz, kein Secret, Beispiel.

## Acceptance Criteria
1. Nach `/bootstrap` ist `## Maschinen-Kontext` in `~/.claude/CLAUDE.md` vorhanden — **ohne** separaten Nutzer-Schritt. ✓
2. Idempotent: bestehender Abschnitt wird nicht ueberschrieben. ✓
3. Inhalt: Typ (OS), Framework (`intentron <VERSION>`), Stack-Praeferenz (A.1), Verfuegbare Skills (`ls ~/.claude/skills/`). ✓
4. `global-registry-update.md` dokumentiert (DE+EN). ✓
5. DE+EN-Paritaet. ✓

## Abgrenzung
Reine Bootstrap-Ergaenzung (Block A) + Doku, kein Sketch. Baut auf BOO-138 (`PROJECTS_ROOT`, v0.8.0) auf.

## Result
Umgesetzt im Branch `feat/boo-145-maschinen-kontext`. DE+EN.
