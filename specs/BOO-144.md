# BOO-144 — Runbook: SecondBrain-Setup nachziehen ohne Re-Bootstrap

## Summary
Eigenes, aus dem HANDBUCH verlinktes Runbook (`docs/runbooks/secondbrain-nachziehen.md` + `.en.md`) für den Fall „Framework schon installiert, will aber **nur** das Leichtgewicht-SecondBrain-Setup (BOO-138/139) nachziehen" — **ohne** `/bootstrap` neu zu fahren. Kern ist ein fertiger, **idempotenter Operator-Prompt**, der die globale `~/.claude/CLAUDE.md` um `PROJECTS_ROOT` ergänzt und bestehende Projekte um `journal/daily/` + Session-Start-/Ende-Routine nachrüstet. Plus Sketch (DE+EN) und HANDBUCH-Verweis als **4. Onboarding-Weg** in Anhang U.

## Why
Die drei Onboarding-Wege in Anhang U decken **neu / 2..N / Bestands-Projekt** ab — aber nicht den **Single-Feature-Nachzug**. Kundenfall: „Ich habe das Framework, will nur die Doku auf Next-Level (SecondBrain) heben, ohne Re-Bootstrap." Kundentauglicher Self-Service-Pfad.

## What
- **`docs/runbooks/secondbrain-nachziehen.md` (+ `.en.md`):** Kontext, Was-passiert, fertiger Operator-Prompt (Teil A Maschinen-Ebene `PROJECTS_ROOT`, Teil B pro Projekt `journal/daily/` + Session-Ende-Routine), Idempotenz-/Sicherheits-Regeln, Sketch-Embed.
- **Sketch `docs/assets/secondbrain-nachziehen.{excalidraw,png}` (+ `.en.*`):** Vorher → Prompt → Nachher (globale CLAUDE.md + Projekt-Loop), OWLIST-Farben, Render-Loop, Read-verifiziert.
- **HANDBUCH Anhang U (DE+EN):** „Weg 4 — Nur SecondBrain nachziehen (ohne Re-Bootstrap)" + Runbook in der Verweise-Liste.

## Acceptance Criteria
1. Runbook DE+EN mit fertigem, idempotentem Operator-Prompt (Read-vor-Edit, Pfad-Bestätigung, kein Secret). ✓
2. Sketch DE+EN (OWLIST, Render-Loop, Read-verifiziert). ✓
3. HANDBUCH Anhang U „Weg 4" + Runbook-Verweis (DE+EN). ✓
4. Release Note Wave AZ (DE+EN). ✓
5. DE+EN-Parität. ✓

## Abgrenzung
Kein neuer Skill, keine Skill-Logik-Änderung — reine Doku + Sketch + Operator-Prompt. Baut auf BOO-138/139 (v0.8.0) auf.

## Result
Umgesetzt im Branch `feat/boo-144-secondbrain-nachziehen`. DE+EN.
