# BOO-132 — Bootstrap A.3b: GitHub Issues als empfohlenen Standard markieren

## Summary
A.3b (Backlog-Adapter) markiert `b) GitHub Issues` als **empfohlenen Standard** (schlank, nativ im Repo,
kein OAuth-Tunnel). Frage-Mechanik unverändert; Default bleibt `none`, aber mit Empfehlungs-Hinweis bei GitHub-Repo.

## What
- `bootstrap/SKILL.md` + `.en.md`, A.3b: Option b) annotiert + `Default: none (Empfehlung bei GitHub-Repo: b)`.

## Acceptance Criteria
1. GitHub Issues sichtbar als empfohlener Standard. ✓
2. Mechanik nicht geändert (kein Zwang). ✓  3. DE+EN. ✓

## Result
Branch `feat/boo-doku-haertung`. Linear bleibt eine gleichberechtigte Option (BOO-133-Runbook deckt die VPS-Komplexität ab).
