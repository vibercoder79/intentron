# BOO-168 — Claude-Code-Modus-Empfehlung + /sprint-run „Zwei Modi"-Klarstellung (DE+EN)

## Summary
Doku-Klarstellung nach Operator-Verwechslung: `/sprint-run` (Default) plant + holt **eine** Freigabe + führt dann den ganzen Sprint aus; `--auto` lässt nur diese Freigabe weg — beide führen real aus, es gibt **keinen** Nur-Prüf-Modus. Plus ein neuer HANDBUCH-§6-Abschnitt „Welcher Claude-Code-Modus zu welchem Skill?" und die Abgrenzung Skill-Plan ≠ Claude-Code-Planungsmodus (read-only, blockiert Umsetzung). Reine Doku, keine Logik-/Versionsänderung.

## Why
Ein Nutzer hielt `/sprint-run` für „nur prüfen" und `--auto` für „das Ausführende" und verwechselte den skill-eigenen Sprint-Plan mit dem Claude-Code-Planungsmodus. Ein Doku-Audit (DE+EN, + /implement-Vergleich) bestätigte: faktisch korrekt, aber missverständlich (überladener „Daemon"-Begriff, kein Default-vs-`--auto`-Vergleich, keine Modus-Empfehlung). Claude-Code-Fakten gegen die offizielle Doku verifiziert (6 Modi; Plan Mode read-only).

## What
- **HANDBUCH.md + .en.md §6**: neuer Abschnitt „Claude-Code-Modus: welcher Berechtigungs-Modus zu welchem Skill?" — Denk-Skills → `plan`; Umsetzung beaufsichtigt → `acceptEdits`; unbeaufsichtigt (`--auto`) → `dontAsk` + Allowlist (`bypassPermissions` nur isoliert); nie Plan Mode für Umsetzung; Abgrenzung + Sicherheitsnetz.
- **sprint-run/README.md + .en.md**: „Zwei Modi"-Block (Default vs `--auto`, beide führen aus, kein Nur-Prüf-Modus) + Plan-Mode-Abgrenzung + Claude-Code-Modus-Empfehlung.
- **sprint-run/SKILL.md + .en.md**: Daemon-Modus-Abschnitt entschärft (Default-Loop läuft nach Freigabe ebenso durch) + Modus-Blockquote.
- **docs/runbooks/sprint-run.md + .en.md**: Klarstellung beide Modi führen aus + Modus-Tipp.
- **implement/README.md + .en.md**: Claude-Code-Modus-Zeile (gleiche `--auto`-Logik).

## Acceptance Criteria
1. „Zwei Modi"-Block + Plan-Mode-Abgrenzung in sprint-run/README (DE+EN). ☐
2. Neuer HANDBUCH-§6-Abschnitt (DE+EN) mit Modus-Tabelle pro Skill-Phase. ☐
3. Modus-Zeile in sprint-run + implement README (DE+EN), Verweis auf HANDBUCH §6. ☐
4. SKILL + Runbook entschärft (DE+EN). ☐
5. `docs-drift-check` grün; kein Versions-Bump (sprint-run bleibt v1.1.0). ☐
6. Release-Note DE+EN. ☐

## Constraints
- Nur Doku; keine Skill-Logik-Änderung; DE+EN-Parität; `change_type: content`; Modus-Fakten verifiziert.

## Abgrenzung
- Wave-Buchstabe wird spät gegen `docs/releases/` vergeben (Cross-Session-Drift-Guard). Repo-Slot BOO-168 = Linear BOO-168.

## Execution Isolation
```yaml
estimate: 3
token_estimate: 16000
execution_mode: sub-agents
worktree_strategy: git-worktree
```

## Session-Referenz
- Branch: `feat/boo-168-claude-mode-docs`
- Session: /implement, 2026-06-06

## Result
_(nach Abschluss ergänzen)_
