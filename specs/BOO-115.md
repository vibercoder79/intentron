# BOO-115 — Tool-Install-Führung mit HANDBUCH-Deeplinks + profil-abhängiger Install-Default

## Summary
Bei erkannt-fehlenden Tools gibt der Bootstrap **Tool-Name + HANDBUCH-Deeplink** aus (statt nur WARN)
und leitet den **Install-Default** aus `DEPLOYMENT_SCENARIO` ab: `solo-mac` → System-Install,
`other` (VPS/Coding-Factory/Team) → Container/Docker (Golden Image). Plus Docker-Preflight, korrekte
Flag-/Verify-Sequenz je Modus und explizit kommunizierter „scaffold-only"-Charakter.

## Why
Probelauf-Punkt #3 + Docker-Default-Entscheidung (2026-06-02): Der Bootstrap scaffoldet bewusst nur
Configs, kommunizierte das aber nicht — Fehlendes erfuhr der Operator erst durchs `verify-setup.sh`-WARN
am Ende, ohne Anleitung. Install-Sequenzen existierten (HANDBUCH Anhang Y.2 + Container-Profil BOO-81),
waren aber vom Bootstrap nicht verlinkt. `DEPLOYMENT_SCENARIO` war rein informativ (`SKILL.md:325`:
„kein Skill-Verhalten haengt davon ab"). BOO-81 hatte System-Install als globalen Default; diese Story
macht den Default **profil-gesteuert**.

## What
- **A.7** (`SKILL.md` + `.en.md`): `INSTALL_DEFAULT` aus `DEPLOYMENT_SCENARIO` ableiten (solo-mac→system,
  other→docker); Konsequenz-Zeile von „informativ" auf „steuert Install-Default" korrigiert.
- **Phase 7.3b** (`SKILL.md` + `.en.md`): Tool-Install-Fuehrung — fehlendes Tool → Name + Deeplink je
  `INSTALL_DEFAULT`; System → Anhang Y.2 + Sequenz `generate-environment-json.sh --force` dann
  `verify-setup.sh`; Docker → §Container-Profil (BOO-81) + Docker-Preflight (`docker --version`) + im
  Container nur `verify-setup.sh` (postCreateCommand triggert `--force`). scaffold-only explizit.
- **Deeplink-Ziele** sind die bestehenden stabilen `### Y.2`-/`### Container-Profil (BOO-81)`-Header
  (DE+EN) — durch Verlinkung aus dem Flow auffindbar gemacht, **nicht** dupliziert (Leichtgewicht).

## Acceptance Criteria
1. Install-Default aus `deployment_scenario` (factory/team→Docker, solo-mac→System). ✓
2. Fehlende Tools → Tool-Name + HANDBUCH-Deeplink (Anhang Y.2 / BOO-81-Profil, DE/EN). ✓
3. Docker-Preflight `docker --version` → vorhanden: go; fehlt: HANDBUCH-Verweis. ✓
4. Korrekte Flag-/Verify-Sequenz je Modus dokumentiert + im Flow ausgegeben. ✓
5. „scaffold-only" explizit kommuniziert. ✓ (Phase 0.1/0.2 + 7.3b)

## Result
Umgesetzt im Branch `feat/boo-bootstrap-flow-hardening`. Verzahnt mit BOO-114 (Pre-Flight nennt scaffold-
only + generische Deeplinks) und BOO-123 (`gh` als Tool). Keine Content-Duplikation der Install-Sequenzen
— der Bootstrap deeplinkt auf die vorhandenen HANDBUCH-Anker.
