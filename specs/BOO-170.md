# BOO-170 — Model-/Permission-Modus-Routing im sprint-run-Daemon (recommended_model → --model) + Spike

## Summary
Macht aus der bisher reinen Doku-Empfehlung (`recommended_model`, BOO-84; Permission-Modus, BOO-168/169) ein **erzwungenes Routing im `/sprint-run --auto`-Daemon**: pro Story wird `/implement` als Subprozess mit aufgelöstem `--model` und `--permission-mode dontAsk` gestartet. Plus Policy-Korrektur: Produktcode läuft auf dem besten Modell (implement → opus). Interaktiv bleibt es bewusst Empfehlung (Variante A, leichtgewichtig — kein ausführbarer Daemon gebaut).

## Why
`recommended_model` wurde bisher nur vom Migrations-Skript geschrieben, von keinem Runtime-Pfad gelesen — reine Doku. Operator-Wunsch: die Empfehlung im unbeaufsichtigten Lauf automatisch anwenden, und der Code-Kern soll auf dem besten Modell laufen (Qualität vor Kosten beim Produktcode; mechanische Loops bleiben günstig).

## Spike-Ergebnis (verifiziert)
- **CLI real geprüft** (`claude --help`): `--model <alias|name>` ✅ und `--permission-mode` ✅ mit choices `acceptEdits, auto, bypassPermissions, default, dontAsk, plan`. **Kein** `--effort`-Flag.
- `sprint-run` ist **prompt-getrieben**, kein ausführbares Daemon-Skript im Repo. Schritt 4.3 war unspezifiziert → wird zur konkreten Subprozess-Invocation präzisiert (Variante A).
- **Grenze:** ein einzelnes `--model` pro Subprozess bildet implement-internes Multi-Tier (haiku-Loops / opus-Security) **nicht** ab → echte interne Trennung = Folge-Story (Subagent-Routing mit `model:` je Subagent).

## What
- **Helper `sprint-run/scripts/resolve-model.py`** (dependency-frei, getestet): `<skill>/SKILL.md recommended_model` (Tier) → `bootstrap/references/model-tiers.json current_version` (Version). Fallback `sonnet`, `--repo-root`/`--tier`-Flags.
- **sprint-run Schritt 4.3 (DE+EN)** konkretisiert: `/implement` als Subprozess mit `--model "$(resolve-model.py implement --repo-root .)" --permission-mode dontAsk` + neuer Abschnitt „Modell-/Modus-Routing (BOO-170)" inkl. Override-Hierarchie, Multi-Tier-Grenze, Daemon-vs-interaktiv.
- **Policy implement → opus (DE+EN):** `recommended_model: sonnet → opus` (Code-Kern bestes Modell); `model-tiers.json` opus.default_for_skills + Beschreibung erweitert; Iterations-Loops bleiben haiku, Security-Findings 6e opus.
- **HANDBUCH (DE+EN):** Enforcement-Absatz im Model-Routing-Anhang + opus-Routing-Zeile erweitert (Produktcode + security-architect).
- **Versions-Bumps:** implement 2.13.0 → 2.14.0, sprint-run 1.1.0 → 1.2.0 (SKILL + README je DE+EN).

## Acceptance Criteria
1. Helper resolve-model.py gebaut + getestet (implement→opus, single-tier→sonnet, Fallback). ✅
2. sprint-run Schritt 4.3 + Routing-Abschnitt (DE+EN). ✅
3. implement recommended_model opus + model-tiers.json (DE+EN). ✅
4. Override-Hierarchie + Multi-Tier-Grenze dokumentiert; interaktiv bleibt Empfehlung. ✅
5. HANDBUCH-Enforcement-Doku (DE+EN). ✅
6. `docs-drift` grün; Versions-Parität gehalten; Wave-Doc. ☐ (im Lauf)

## Constraints
- Variante A (leichtgewichtig): Helper + Subprozess-Spezifikation, **kein** vollausführbarer Daemon. Bestehende SSoT (Frontmatter + model-tiers.json), kein neues Konfig-Feld. DE+EN-Parität. Anti-Fabrikation: CLI-Fakten gegen `claude --help` verifiziert.

## Abgrenzung
- **Folge-Story (offen):** implement-internes Subagent-Modell-Routing (haiku-Loops / opus-Security innerhalb eines Laufs) + vollausführbarer VPS-Daemon (Variante B). Effort bleibt nicht erzwingbar (kein CLI-Flag).
- Wave-Buchstabe spät gegen `docs/releases/` vergeben. Repo-Slot BOO-170 = Linear BOO-170.

## Execution Isolation
```yaml
estimate: 5
token_estimate: 30000
execution_mode: sub-agents
worktree_strategy: git-worktree
```

## Session-Referenz
- Branch: `feat/boo-170-daemon-model-routing`
- Session: Lead-orchestriert, 2026-06-06

## Result
Umgesetzt 2026-06-06 (Variante A). Helper getestet (`implement → claude-opus-4-7`, `backlog/sprint-review → claude-sonnet-4-6`, Fallback `sonnet`). Interaktives Enforcement + voller Daemon bleiben Folge-Arbeit.
