# v0.10.1 — /sprint-run: Post-Story-Gate-Assertion (BOO-165)

**Highlight:** `sprint-run` v1.1.0 — neuer **Schritt 4.5b**: nach jedem `/implement`-Lauf verifiziert der Orchestrator **maschinell** gegen die `meta.json` (BOO-36/84), dass kein Pflicht-Gate still übersprungen wurde. Merge nur bei **grüner CI _und_ grüner Assertion**. Schließt die Lücke zwischen prompt-getriebener Gate-Ausführung (Ebene 1) und Remote-CI-Gate (BOO-148, Ebene 2).

## Wave BK — Post-Story-Gate-Assertion (BOO-165)

- `sprint-run/SKILL.md` (+EN): Schritt 4.5b + Legitimitäts-Matrix (Skip nur legitim, wenn durch `change_type`/Non-Code gedeckt **oder** in `override_audit` belegt; fehlende `meta.json` → Fail). v1.0.0 → **1.1.0**.
- Neu `sprint-run/references/gate-assertion.md` (+EN).
- `sprint-run/README.md` (+EN) + HANDBUCH **Anhang AD** (DE+EN) + Runbook (DE+EN) nachgezogen.
- Sketches `sprint-run-flow` + `story-breakdown` (DE+EN) zeigen den Assertion-Knoten zwischen CI und Merge.

Details: `docs/releases/wave-bk-gate-assertion.md` (+ `.en.md`).

## Hotfix in diesem Release

- **`wave-bi-role-runbooks` → `wave-bj-role-runbooks`**: #58 (BOO-157) und #59 (BOO-158) hatten beide Wave `bi` belegt → docs-drift war auf `main` rot. `sprint-run` ist als v0.10.0 released (unveränderbar), daher zog `role-runbooks` auf `bj`. `main` ist wieder grün.

## Ebenfalls auf diesem Tag (seit v0.10.0)

- **BOO-158** (#59) — rollenspezifische Runbooks (CISO/DPO/CTO/Geschäftsführung), DE+EN. → `docs/releases/wave-bj-role-runbooks.md`

## Verifikation

`docs-drift-check` grün (0 Befunde) · DE/EN-Parität · SKILL↔README v1.1.0.

---

# 🇬🇧 English Version

# v0.10.1 — /sprint-run: post-story gate assertion (BOO-165)

**Highlight:** `sprint-run` v1.1.0 — new **step 4.5b**: after every `/implement` run the orchestrator **machine-verifies** against the `meta.json` (BOO-36/84) that no mandatory gate was silently skipped. Merge only on **green CI _and_ green assertion**. Closes the gap between prompt-driven gate execution (layer 1) and the remote-CI gate (BOO-148, layer 2).

## Wave BK — post-story gate assertion (BOO-165)

- `sprint-run/SKILL.md` (+EN): step 4.5b + legitimacy matrix (a skip is legitimate only if covered by `change_type`/non-code **or** recorded in `override_audit`; missing `meta.json` → fail). v1.0.0 → **1.1.0**.
- New `sprint-run/references/gate-assertion.md` (+EN).
- `sprint-run/README.md` (+EN) + HANDBUCH **Appendix AD** (DE+EN) + runbook (DE+EN) updated.
- Sketches `sprint-run-flow` + `story-breakdown` (DE+EN) show the assertion node between CI and merge.

Details: `docs/releases/wave-bk-gate-assertion.en.md`.

## Hotfix in this release

- **`wave-bi-role-runbooks` → `wave-bj-role-runbooks`**: #58 (BOO-157) and #59 (BOO-158) both used wave `bi` → docs-drift was red on `main`. `sprint-run` is released as v0.10.0 (immutable), so `role-runbooks` moved to `bj`. `main` is green again.

## Also on this tag (since v0.10.0)

- **BOO-158** (#59) — role-specific runbooks (CISO/DPO/CTO/managing director), DE+EN. → `docs/releases/wave-bj-role-runbooks.en.md`

## Verification

`docs-drift-check` green (0 findings) · DE/EN parity · SKILL↔README v1.1.0.
