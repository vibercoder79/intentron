# v0.10.0 — /sprint-run: Sprint-Orchestrator (BOO-157)

**Highlight:** Neuer Orchestrator-Skill **`/sprint-run`** fährt einen ganzen Sprint vollautomatisch — er wählt Stories aus dem priorisierten Backlog, setzt jede per `/implement` im Daemon-Modus um (eigener `git worktree` + Branch), pflegt den Linear-Status, wartet auf grüne Remote-CI, merged, räumt auf und triggert am 80%-Token-Boundary `/sprint-review`. Reiner Orchestrator — `/implement`, `/backlog`, `/sprint-review` bleiben unverändert.

## Wave BI — /sprint-run (BOO-157)

- **Neu `sprint-run/`** — `SKILL.md` + `README.md` (v1.0.0) + 4 `references/` (orchestration-checklist, gate-block-handling, worktree-flow, token-boundary), je DE + EN.
- **5 Owlist-Sketches** + `overview` (`.excalidraw` + `.png`, DE + EN): Sprint-Run-Flow, Story-Breakdown, Agent-Interaktion, GitHub-Integration, Gate-Block-Handling.
- **HANDBUCH** (DE + EN): `/sprint-run`-Eintrag in §6 + **Anhang/Appendix AD** mit allen 5 Sketches.
- **`docs/runbooks/sprint-run.md`** (DE + EN): komplette Skill-Kette `intent → ideation → backlog → sprint-run → implement → sprint-review`.
- README-Cross-Refs (`/backlog`, `/implement`, `/sprint-review`, `/ideation`).
- Governance bleibt strikt: Gate-Blocks (sensitive-paths / personal-data) pausieren und werden **nie** automatisch überbrückt; kein Merge ohne grüne CI (BOO-148).

Details: `docs/releases/wave-bi-sprint-run.md` (+ `.en.md`).

## Ebenfalls in diesem Release (seit v0.9.5 auf `main` aufgelaufen)

Diese Änderungen lagen bereits auf `main`, ohne eigenen Tag — `v0.10.0` markiert ihren Stand mit. Eigene Wave-Notes verlinkt:

- **BOO-155** (Wave BF) — Upgrade-Doku nachgezogen (`/bootstrap --update` entfernt, `framework-upgrade.md` ergänzt). → `docs/releases/wave-bf-upgrade-doku-nachziehen.md`
- **BOO-156** (Wave BG) — Framework-Update-Runbook für Bestandsprojekte. → `docs/releases/wave-bg-framework-update-runbook.md`
- **knowledge-onboarding v1.1.0** (#57) — 5 Erklär-Sketches (DE + EN).
- **SonarCloud-/Vercel-CI-Runbooks** + Diagramme unter `docs/runbooks/`.
- **framework-upgrade** — Schritt 4a „neue Skills inventarisieren" in `bootstrap/references/framework-upgrade.md`.

## Verifikation

`docs-drift-check` grün (0 Befunde) · DE/EN-Parität · SKILL↔README v1.0.0 · CI grün (PR #58).

---

# 🇬🇧 English Version

# v0.10.0 — /sprint-run: sprint orchestrator (BOO-157)

**Highlight:** A new orchestrator skill **`/sprint-run`** runs a whole sprint fully automatically — it picks stories from the prioritized backlog, implements each via `/implement` in daemon mode (its own `git worktree` + branch), maintains Linear status, waits for green remote CI, merges, cleans up and triggers `/sprint-review` at the 80% token boundary. Pure orchestrator — `/implement`, `/backlog`, `/sprint-review` stay unchanged.

## Wave BI — /sprint-run (BOO-157)

- **New `sprint-run/`** — `SKILL.md` + `README.md` (v1.0.0) + 4 `references/` (orchestration-checklist, gate-block-handling, worktree-flow, token-boundary), DE + EN each.
- **5 Owlist sketches** + `overview` (`.excalidraw` + `.png`, DE + EN): sprint-run flow, story breakdown, agent interaction, GitHub integration, gate-block handling.
- **HANDBUCH** (DE + EN): `/sprint-run` entry in §6 + **Appendix AD** with all 5 sketches.
- **`docs/runbooks/sprint-run.md`** (DE + EN): full skill chain `intent → ideation → backlog → sprint-run → implement → sprint-review`.
- README cross-refs (`/backlog`, `/implement`, `/sprint-review`, `/ideation`).
- Governance stays strict: gate blocks (sensitive-paths / personal-data) pause and are **never** bypassed automatically; no merge without green CI (BOO-148).

Details: `docs/releases/wave-bi-sprint-run.en.md`.

## Also in this release (accumulated on `main` since v0.9.5)

These changes were already on `main` without their own tag — `v0.10.0` marks their state. Their own wave notes are linked:

- **BOO-155** (Wave BF) — upgrade docs caught up (`/bootstrap --update` removed, `framework-upgrade.md` extended). → `docs/releases/wave-bf-upgrade-doku-nachziehen.en.md`
- **BOO-156** (Wave BG) — framework-update runbook for existing projects. → `docs/releases/wave-bg-framework-update-runbook.en.md`
- **knowledge-onboarding v1.1.0** (#57) — 5 explainer sketches (DE + EN).
- **SonarCloud / Vercel CI runbooks** + diagrams under `docs/runbooks/`.
- **framework-upgrade** — step 4a "inventory new skills" in `bootstrap/references/framework-upgrade.md`.

## Verification

`docs-drift-check` green (0 findings) · DE/EN parity · SKILL↔README v1.0.0 · CI green (PR #58).
